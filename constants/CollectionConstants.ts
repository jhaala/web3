export const MINIMUM_WIDTH = 1200;
export const MINIMUM_HEIGHT = 1200;

export const MAX_SIZE = 104857600; // 100MB
export const MAX_FILES = 1;

export interface BuildCollectionInputType {
  name: string;
  slug: string;
  description: string;
  keywords: string;
}
